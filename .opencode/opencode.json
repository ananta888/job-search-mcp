{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "job-crawler": {
      "type": "local",
      "command": [
        "bash",
        "-lc",
        "if [ -x .venv/bin/job-search-mcp ]; then exec .venv/bin/job-search-mcp; elif command -v job-search-mcp >/dev/null 2>&1; then exec job-search-mcp; else export PYTHONPATH=src; exec python -m job_search_mcp.interfaces.mcp_server; fi"
      ],
      "cwd": ".",
      "enabled": true,
      "environment": {
        "ALLOW_EXTERNAL_PORTALS": "1"
      }
    }
  }
}
